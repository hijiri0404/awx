- name: Stop instances
  hosts: localhost
  tasks:
    - name: Stop webserver1
      ec2:
        instance_tags:
            Name: "{{ tag }}"
        state: stopped
        region: ap-northeast-1
      register: cmd_out

    - name: debug
      debug:
        var: cmd_out
